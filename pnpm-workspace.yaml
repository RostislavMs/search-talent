packages:
  - "."

# pnpm 11 requires an explicit boolean decision for dependency build scripts.
# Supabase's postinstall downloads bin/supabase.EXE used by its package bin.
allowBuilds:
  "@google/genai": true
  esbuild: true
  protobufjs: true
  sharp: true
  supabase: true
  unrs-resolver: true
